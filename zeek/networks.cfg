# Zeek local networks configuration
# List your local network subnets in CIDR notation, one per line.
# Zeek uses this to distinguish local from external traffic.
#
# This file is written by scripts/install-zeek.sh using the ZEEK_NETWORK
# environment variable. Edit directly to add additional subnets.
#
# Examples:
#   192.168.1.0/24    Main LAN
#   10.0.0.0/8        VPN range
#   172.16.0.0/12     Docker networks

192.168.1.0/24
